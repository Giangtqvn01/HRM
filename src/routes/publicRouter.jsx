import Dashboard from 'pages/Dashboard'
const routes = [
    {
        path: '/dashboard',
        component: Dashboard,
        title: 'Home',
        needsAuth: false,
    }
]

export default routes;